dir:
  app: '/home/vagrant/app'
  web: '/home/vagrant/app/web'
  cache: '/home/vagrant/cache'
  log: '/home/vagrant/log'

php:
    install: true
    version: '5.5'
    dev: true
    libpcre: true
    symfony2: false

mysql:
    install: true

nodejs:
    install: true

apache:
    install: true
    docroot: "%{web_dir}"
    hostname: 'site.dev'

mongodb:
    install: true